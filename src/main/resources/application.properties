spring.application.name=Backend WaveHeaven

# Database Configuration - PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/waveheaven
spring.datasource.username=postgres
spring.datasource.password=Vegetta777
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Server Configuration
server.port=8080

# JSON Configuration
spring.jackson.serialization.fail-on-empty-beans=false

# JWT Configuration
jwt.secret=waveheaven-clave-secreta-para-jwt-token-la-revolucion-industrial-y-sus-consecuencias-han-sido-un-desastre-para-la-raza-humana
jwt.expiration=86400000
